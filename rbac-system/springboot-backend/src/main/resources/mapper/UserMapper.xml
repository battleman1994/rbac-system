<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rbac.mapper.UserMapper">
    
    <resultMap id="userWithRolesMap" type="com.rbac.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="avatar" property="avatar"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="last_login_at" property="lastLoginAt"/>
        <collection property="roles" ofType="com.rbac.entity.Role">
            <id column="role_id" property="id"/>
            <result column="role_name" property="roleName"/>
            <result column="role_code" property="roleCode"/>
            <result column="role_description" property="description"/>
            <result column="role_status" property="status"/>
        </collection>
    </resultMap>
    
    <select id="selectByUsernameWithRoles" resultMap="userWithRolesMap">
        SELECT 
            u.*,
            r.id as role_id,
            r.role_name,
            r.role_code,
            r.description as role_description,
            r.status as role_status
        FROM sys_user u
        LEFT JOIN sys_user_role ur ON u.id = ur.user_id
        LEFT JOIN sys_role r ON ur.role_id = r.id
        WHERE u.username = #{username}
    </select>
    
    <select id="selectUserList" resultType="com.rbac.entity.User">
        SELECT * FROM sys_user
        <where>
            <if test="username != null and username != ''">
                AND username LIKE CONCAT('%', #{username}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY created_at DESC
    </select>
    
    <update id="updateLastLoginTime">
        UPDATE sys_user SET last_login_at = NOW() WHERE id = #{id}
    </update>
</mapper>
